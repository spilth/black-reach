import React from "react";

export const Signum = () => {
  return <>
    <p>If you have at least one Signum Unit with a Clear LoS
      to the Interspace targeted by an Indirect Fire template,
      reduce the scattering distance by 1 Interspace.</p>
  </>;
};