import React from "react";

export const PersonalOrder = () => {
  return <>
    <p>This Unit provides one additional Order Token, just
      like the Order Special Ability above, but, you must
      give this Unit an Order Token every turn (numbered,
      Special, or Bluff).</p>
  </>;
};