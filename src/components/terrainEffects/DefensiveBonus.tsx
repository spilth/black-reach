import React from "react";

export const DefensiveBonus = () => {
  return <>
    <p>Any Unit in this Terrain Element receives the bonus
      if the colour of the Unit's Defence Value type is the same as the
      colour of the Terrain Element's symbol.</p>
    <p>Add the bonus printed in the symbol to the Unit's Defence
      Value.</p>
  </>;
};